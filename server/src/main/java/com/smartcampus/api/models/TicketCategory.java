package com.smartcampus.api.models;

/**
 * Category chips shown on the maintenance list (matches the Figma).
 * Stored as a String enum on the Ticket document.
 */
public enum TicketCategory {
    IT_EQUIPMENT,
    FURNITURE,
    HVAC,
    ELECTRICAL,
    SAFETY,
    OTHER
}
